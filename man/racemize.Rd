% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics.R
\name{racemize}
\alias{racemize}
\title{Simulate D/L accumulation over a full temperature history}
\usage{
racemize(age, sumT, winT, x, dT, startingDL = 0, scenario = NA, ...)
}
\arguments{
\item{age}{Numeric vector of ages (ka BP). Sorted old-to-young internally.}

\item{sumT}{Numeric vector of summer half-year temperatures (°C),
same length as \code{age}.}

\item{winT}{Numeric vector of winter half-year temperatures (°C),
same length as \code{age}.}

\item{x}{Power-law exponent. Default \code{3}.}

\item{dT}{Timestep in years.}

\item{startingDL}{Starting D/L value. Default \code{0} (unracemized).}

\item{scenario}{Character label stored in the output. Default \code{NA}.}

\item{...}{Additional arguments passed to \code{\link{arrhenius}}.}
}
\value{
A one-row data frame with columns \code{age} (maximum input age),
\code{DL}, and \code{scenario}.
}
\description{
Integrates racemization from the oldest to the youngest point in a
temperature time series, splitting each timestep into summer and winter
halves. Returns the final D/L ratio after back-transforming from
\eqn{(D/L)^x} space.
}
\examples{
age  <- seq(0, 10, by = 0.01)
sumT <- rep(15, length(age))
winT <- rep(4,  length(age))
racemize(age, sumT, winT, x = 3, dT = 10)

}
\seealso{
\code{\link{racemizeSample}} for a version that also applies
sediment temperature damping; \code{\link{dampSedTemps}} for the
damping model.
}
