% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics.R
\name{racemizeSample}
\alias{racemizeSample}
\title{Simulate racemization for a single downcore sample (with burial damping)}
\usage{
racemizeSample(
  sampleAge,
  ageVec,
  sumT,
  winT,
  x,
  dT,
  startingDL = 0,
  scenario = NA,
  startDamp = 1,
  endDamp = 0.5,
  dampTime = 10,
  ...
)
}
\arguments{
\item{sampleAge}{Age of the sample (ka BP). Only temperature history at or
older than this age is used.}

\item{ageVec}{Numeric vector of ages spanning the full temperature history
(ka BP).}

\item{sumT}{Numeric vector of summer half-year temperatures (°C), same
length as \code{ageVec}.}

\item{winT}{Numeric vector of winter half-year temperatures (°C), same
length as \code{ageVec}.}

\item{x}{Power-law exponent.}

\item{dT}{Timestep in years.}

\item{startingDL}{Starting D/L value. Default \code{0}.}

\item{scenario}{Character label for the output. Default \code{NA}.}

\item{startDamp, endDamp, dampTime}{Passed to \code{\link{dampSedTemps}}.}

\item{...}{Additional arguments passed to \code{\link{arrhenius}}.}
}
\value{
A one-row data frame with columns \code{age}, \code{DL}, and
\code{scenario}.
}
\description{
Full pipeline for one sample: selects the portion of the temperature
history from deposition to present, applies \code{\link{dampSedTemps}} to
attenuate the seasonal signal with burial depth, then integrates
racemization via \code{\link{racemize}}.
}
\examples{
age  <- seq(0, 10, by = 0.01)
sumT <- rep(15, length(age))
winT <- rep(4,  length(age))
racemizeSample(sampleAge = 5, ageVec = age, sumT = sumT, winT = winT,
               x = 3, dT = 10)

}
\seealso{
\code{\link{racemize}}, \code{\link{dampSedTemps}},
\code{\link{racemize_depth_series}} for the more physically rigorous
depth-series workflow.
}
