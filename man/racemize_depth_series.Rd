% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth_series.R
\name{racemize_depth_series}
\alias{racemize_depth_series}
\title{Simulate AAR racemization for a depth series of sediment samples}
\usage{
racemize_depth_series(
  depths_cm,
  age_model,
  temp_model,
  sumT_model = NULL,
  winT_model = NULL,
  dT_yr = 10,
  AAR_params = default_AAR_params,
  kappa_m2yr = 1.6
)
}
\arguments{
\item{depths_cm}{Numeric vector of sample depths in cm.}

\item{age_model}{Age-depth model from \code{\link{make_age_model}} or
\code{\link{rate_to_age_model}}.}

\item{temp_model}{Mean annual lake bottom water temperature: scalar or
\code{data.frame(age_ka, temp_C)}.}

\item{sumT_model}{Optional summer half-year temperature (same format as
\code{temp_model}). Supply with \code{winT_model} for seasonal mode.}

\item{winT_model}{Optional winter half-year temperature.}

\item{dT_yr}{Timestep in years. Default \code{10}.}

\item{AAR_params}{Named list of kinetic parameters. Default
\code{\link{default_AAR_params}}.}

\item{kappa_m2yr}{Sediment thermal diffusivity in m\eqn{^2}/yr. Default
\code{1.6}.}
}
\value{
A data frame with columns \code{depth_cm}, \code{deposition_age_ka}, \code{DL}.
}
\description{
Applies \code{\link{racemize_one_depth}} over a vector of sample depths
and returns the results as a single data frame. This is the primary entry
point for forward-modelling a downcore D/L profile.
}
\examples{
am     <- rate_to_age_model(rate_cm_per_ka = 50, max_depth_cm = 500)
depths <- seq(0, 500, by = 10)
out    <- racemize_depth_series(depths, age_model = am, temp_model = 5)
head(out)

}
\seealso{
\code{\link{racemize_one_depth}} for the single-sample function
and full documentation of the two temperature modes.
}
