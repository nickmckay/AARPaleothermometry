% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{posterior_predictive_DL}
\alias{posterior_predictive_DL}
\title{Posterior predictive D/L depth profiles}
\usage{
posterior_predictive_DL(
  mcmc_out,
  age_model,
  depths_cm,
  temp_model,
  AAR_params_fn = function(row) {
     list(Ae = exp(row[["log_Ae"]]), Ea = row[["Ea"]],
    R = 0.001987, x = row[["x"]])
 },
  observed_data = NULL,
  burnin = 1000,
  n_draws = 200
)
}
\arguments{
\item{mcmc_out}{List returned by \code{\link{run_mcmc}}.}

\item{age_model}{Age-depth model.}

\item{depths_cm}{Numeric vector of depths at which to evaluate predictions.}

\item{temp_model}{Temperature model for predictions.}

\item{AAR_params_fn}{Function taking one row of the posterior sample matrix
and returning an \code{AAR_params} list. Defaults to extracting \code{log_Ae},
\code{Ea}, \code{x} from the Stage 1 posterior.}

\item{observed_data}{Optional data frame with columns \code{depth_cm} and
\code{DL_obs} to overlay as points.}

\item{burnin}{Integer. Burn-in iterations to discard. Default \code{1000}.}

\item{n_draws}{Integer. Number of posterior draws. Default \code{200}.}
}
\value{
A ggplot object.
}
\description{
Draws parameter sets from the posterior and runs the forward model to
produce an envelope of predicted D/L vs. depth, optionally overlaid with
observed data.
}
