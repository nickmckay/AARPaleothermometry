% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics.R
\name{dampSedTemps}
\alias{dampSedTemps}
\title{Apply sediment burial temperature damping}
\usage{
dampSedTemps(sumT, winT, age, startDamp = 1, endDamp = 0.5, dampTime = 8)
}
\arguments{
\item{sumT}{Numeric vector of summer half-year air temperatures (°C).}

\item{winT}{Numeric vector of winter half-year air temperatures (°C).}

\item{age}{Numeric vector of ages (ka BP), same length as \code{sumT} and
\code{winT}. Sorted old-to-young internally.}

\item{startDamp}{Weight on the same-season temperature at the surface
(most recent). Default \code{1} (no attenuation at surface).}

\item{endDamp}{Weight on the same-season temperature at full burial.
Default \code{0.5} (summer and winter sediment temperatures equal at depth).}

\item{dampTime}{Time scale over which damping is applied (ka). Default \code{8}.}
}
\value{
A data frame with columns \code{age}, \code{sumT}, \code{winT},
\code{sumTsed} (dampened summer sediment temperature), and \code{winTsed}
(dampened winter sediment temperature).
}
\description{
As sediment is buried deeper, the seasonal air temperature signal
attenuates: summer and winter sediment temperatures converge toward the
mean annual. This function models that process as a linear interpolation
from full seasonal amplitude (fresh surface sediment, \code{weight1 = startDamp})
to attenuated signal (deeply buried sediment, \code{weight1 = endDamp}) over
\code{dampTime} ka. The physical motivation is thermal diffusion in the
sediment column.
}
\examples{
age  <- seq(0, 10, by = 0.01)
sumT <- rep(15, length(age))
winT <- rep(4,  length(age))
d <- dampSedTemps(sumT, winT, age)
head(d)

}
\seealso{
\code{\link{racemizeSample}} which calls this function internally;
\code{\link{diffusion_attenuation}} for the physically motivated
exponential depth-based alternative used in the depth-series model.
}
