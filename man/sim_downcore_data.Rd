% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{sim_downcore_data}
\alias{sim_downcore_data}
\title{Simulate downcore D/L observations from a known-temperature lake}
\usage{
sim_downcore_data(
  age_model,
  temp_model,
  sample_ages_ka,
  AAR_params = default_AAR_params,
  sigma = 0.015,
  seed = NULL,
  sumT_model = NULL,
  winT_model = NULL,
  toc_model = NULL,
  f_bac = 0
)
}
\arguments{
\item{age_model}{Age-depth model from \code{\link{make_age_model}} or
\code{\link{rate_to_age_model}}.}

\item{temp_model}{Bottom water temperature history: scalar or
\code{data.frame(age_ka, temp_C)}.}

\item{sample_ages_ka}{Numeric vector of sample ages (ka BP). Converted to
depths via the age model.}

\item{AAR_params}{True kinetic parameters used to generate the data.
Default \code{\link{default_AAR_params}}.}

\item{sigma}{Standard deviation of Gaussian observation noise. Default
\code{0.015}.}

\item{seed}{Optional integer seed.}

\item{sumT_model}{Optional summer half-year temperature.}

\item{winT_model}{Optional winter half-year temperature.}

\item{toc_model}{Optional TOC interpolation function from
\code{\link{make_toc_model}} for bacterial resetting. Default \code{NULL}.}

\item{f_bac}{Bacterial resetting rate constant (yr\eqn{^{-1}}). Default \code{0}.}
}
\value{
A data frame with columns: \code{depth_cm}, \code{deposition_age_ka},
\code{age_ka}, \code{DL_pred}, \code{DL_obs}.
}
\description{
Generates synthetic downcore D/L values by running the forward model
(\code{\link{racemize_depth_series}}) and adding Gaussian noise. Used for
both the 4°C calibration core (Stage 1) and the unknown-temperature target
core (Stage 2).
}
\examples{
am <- rate_to_age_model(rate_cm_per_ka = 50, max_depth_cm = 750)
dc <- sim_downcore_data(am, temp_model = 4,
                        sample_ages_ka = seq(0.5, 15, by = 0.5),
                        seed = 1)
head(dc)

}
